<!DOCTYPE html>
<html lang="en">
<head>
     <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/libs/bootstrap/dist/css/bootstrap.min.css">
    <title> Blogapp</title>
</head>
<body>

    <%- include("../partials/nav")  %> 

    <div class="container mt-3">
        <div class="row">
            <div class="col-md12 mt-3">
                <% if (action != "undefined" && action == "edit") { %>
                    <div class="alert alert-success">
                        <%= blogid %>  id numarali  blog başarıyla güncellendi.
                    </div>
                <% } %>
                <% if (action != "undefined" && action == "create") { %>
                    <div class="alert alert-success">
                        Blog başarıyla eklendi.
                    </div>
                <% } %>
                <% if (action != "undefined" && action == "delete") { %>
                    <div class="alert alert-danger">
                        Blog başarıyla silindi.
                    </div>
                <% } %>
                <table class="table table-bordered table-striped">
                    <thead>
                        <tr>
                            <th style="width: 50px;">id</th>
                            <th style="width: 200px;">resim</th>
                            <th>baslik</th>
                            <th></th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% blogs.forEach(blog => { %>
                         <tr>
                            <td>#<%= blog.blogid %></td>
                            <td><img src="/images/<%= blog.resim %>" class="img-fluid" alt=""></td>
                            <td><%= blog.baslik %></td>
                            <td style="width: 100px;"><a class="btn btn-sm btn-primary" href="/admin/blogs/<%= blog.blogid %>">Düzenle</a></td>
                            <td style="width: 100px;"><a class="btn btn-sm btn-danger" href="/admin/blog/delete/<%= blog.blogid %>">Sil</a></td>
                         </tr>
                        <% }) %>
                    </tbody>
                    
                </table>
            </div>
        </div>
    </div>
    
</body>
</html>